import React from 'react'
import styles from './Together.module.css'

const Together = () => {
  return (
    <div className={`${styles.Togethergen}`}>
        
        <h2 className={`${styles.Together}`}>Together, we can build a better life for all.</h2>

<a className={`${styles.jut}`} href=""> Join Us Today</a>
    </div>
  )
}

export default Together